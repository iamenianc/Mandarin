# Voices pinned for build-time reference generation (Kokoro-82M, lang_code 'z').
# An id is added only once a track uses it. See README.md and ../schema/track.schema.json.
version: 1
engine: kokoro-82m
langCode: z
# Pinned checkpoint: <repo>@<revision> (ADR 0006 requires a pinned checkpoint).
checkpoint: hexgrad/Kokoro-82M@v1.0
# Voice used when no per-item voice is declared; must be listed under voices.
defaultVoice: zf_xiaobei
voices:
  - zf_xiaobei
